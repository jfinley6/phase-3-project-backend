default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  adapter: sqlite3
  database: databse.db

test:
  adapter: sqlite3
  database: database.db

production:
  adapter: postgresql
  database: mission
  username: username
  password: password
  host: localhost
